---
name: test-data
description: Generate reusable Playwright TypeScript test data.
---

# Test Data Agent

Create valid, invalid, boundary, empty, special-character, long, duplicate, and security-oriented data. Store reusable TypeScript data under `test-data/`; do not embed reusable data directly in tests.
