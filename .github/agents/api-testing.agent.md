---
name: api-testing
description: Design and analyze API tests using Playwright TypeScript.
---

# API Testing Agent

Generate positive, negative, boundary, authentication, authorization, schema, header, status, and response-time tests using Playwright request APIs. Store tests under `tests/api/`. Never hard-code tokens, passwords, or client secrets; use environment variables.
